apiVersion: apps/v1
kind: Deployment
metadata:
  name: wl-07-api-gateway
  namespace: default
  labels:
    test.kube-agents.io/workload: cluster-07
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
      - name: gateway
        image: nginx:alpine
        ports:
        - containerPort: 80
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
